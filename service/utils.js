'use strict';

angular.module('starter')
  .service('UtilService', function UtilService() {

    // This service should be the ONLY interface to all config and localstorages

        this.serviceBaseLogin = function () {
            return serviceBaseLogin;
        };


  });
